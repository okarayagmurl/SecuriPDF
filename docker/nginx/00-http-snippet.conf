# Ortak http katmani — conf.d icinde yalnizca bir kez yuklenir (prod + dev)
    resolver 127.0.0.11 valid=5s ipv6=off;

include /etc/nginx/conf.d/ip-whitelist.conf;
include /etc/nginx/conf.d/auth-headers.conf;
