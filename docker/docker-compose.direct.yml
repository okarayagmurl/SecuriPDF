# SecuriPDF — Auth OLMADAN doğrudan erişim (yalnızca yerel geliştirme)
# Kullanım: docker compose -f docker-compose.yml -f docker-compose.direct.yml up -d
# UYARI: Prod veya auth testinde KULLANMAYIN — docker-compose.auth.yml kullanın.

name: entera-pdf

services:
  nginx:
    ports:
      - "${HTTP_PORT:-8080}:8080"
