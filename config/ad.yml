# Active Directory / LDAP — sablon (musteri degerleri Admin panelden girilir)
# Offline pakette Entera lab bilgisi OLMAMALI.
#
# Admin → Yapılandırma → LDAP kaydet → Keycloak'a uygula

connection:
  host: ""
  url: ""
  url_tls: ""
  base_dn: ""
  users_dn: ""
  bind_dn: ""

keycloak_ldap_settings:
  vendor: Active Directory
  username_ldap_attribute: sAMAccountName
  rdn_ldap_attribute: cn
  uuid_ldap_attribute: objectGUID
  user_object_classes: person, organizationalPerson, user
  edit_mode: READ_ONLY
  sync_registrations: false

groups:
  user: SecuriPDF-Users
  admin: SecuriPDF-Admins

realm: securipdf
